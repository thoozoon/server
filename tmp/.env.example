# Email Configuration for Resend API
# Copy this file to .env and fill in your actual values

# Resend API Key (get this from https://resend.com dashboard)
RESEND_API_KEY=re_your_resend_api_key_here

# Email address to use as sender (must be verified in Resend or use onboarding@resend.dev for testing)
FROM_EMAIL=onboarding@resend.dev

# JWT Secret (change this in production!)
JWT_SECRET=your-very-secure-secret-key-change-this-in-production

# Database path (optional, defaults to users.db)
DB_PATH=users.db

# Server port (optional, defaults to 8080)
PORT=8080

# RESEND SETUP INSTRUCTIONS:
# 1. Sign up at https://resend.com
# 2. Create an API key in the API Keys section
# 3. For testing: use FROM_EMAIL=onboarding@resend.dev (no verification needed)
# 4. For production: verify your domain and use FROM_EMAIL=noreply@yourdomain.com

# Example configurations:

# Development/Testing:
# RESEND_API_KEY=re_your_api_key_here
# FROM_EMAIL=onboarding@resend.dev

# Production with verified domain:
# RESEND_API_KEY=re_your_api_key_here
# FROM_EMAIL=noreply@yourdomain.com

# Security Notes:
# - Never commit the actual .env file to version control
# - The API key provides full access to your Resend account, keep it secure
# - Use a strong JWT_SECRET in production
# - Verify your domain with Resend for better deliverability in production
